import { ResourceArticle } from "@/backend/types";

/**
 * Psychoeducation Resource Library Queries (Stub)
 * 
 * TODO: Implement real database query functions once DB provider is selected.
 */

export async function fetchResources(searchQuery?: string): Promise<ResourceArticle[]> {
  // TODO: Replace with real database query
  return [];
}
